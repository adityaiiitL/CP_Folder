#include<bits/stdc++.h>
using namespace std;
int main()
{
    //to print a character n tumber of times-->string s(n,'character')
   
    string s;
    cin>>s;
    
    //to read the whole sentence --> getline(cin,s);
    
    //to append two strings --> s1+s2  OR  s1.append(s2)
    
    //to replace the whole string with another string value -->s.assign("new name")
    
    //clear a string -->s.clear()
    
    //compare two strings -->s1.compare(s2)
    
    //delete a substring part --> s.erase(starting point,number of chars)
    
    //find a substring -->s.find("name os substring") OR s.substr(starting point, number of chars)
    
    //trim the size of string-->s.resize(size of final string);
    
    //convert string to integer-->stoi(s);

    //covert int to string--> int x; cin>>x; to_string(x);
    
    //to sort--> sort(s.begin(),s.end());
    
    return 0;
}