#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        char s[9];
        cin.getline(s,8);
        int n;
        cin>>n;
        while(n--)
        {
            char a[9];
            cin.getline(a,8);
            char b[9];
            cin.getline(b,8);
            cout<<"YES";
        }
    }
    return 0;
}