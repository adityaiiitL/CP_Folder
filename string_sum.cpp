#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    string x,y;
    cin>>x>>y;
    int n=max(x.size(),y.size());
    int k=0;
    for(int i=0;i<n;i++)
    {
        int a=x[i]-48,b=y[i]-48;
        string s=to_string(a+b);
        a[i]=
    }
    return 0;
}